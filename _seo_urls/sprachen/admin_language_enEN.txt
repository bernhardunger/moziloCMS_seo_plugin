# Language file: seo_urls plugin – Admin info (English)
info_intro = <p>Converts category and page names into SEO-friendly URL slugs. Runs as <code>plugin_first</code> – before <code>createGetCatPageFromModRewrite()</code>.</p>
info_requirements = <b>Requirements</b><table>  <tr><td><b>moziloCMS</b></td><td>3.0.x or higher (moziloCMS 2.x is not supported)<br>    <small style="color:#666;">Note on the version string &ldquo;2.0&nbsp;/&nbsp;3.0&rdquo;: moziloCMS checks internally whether the version string contains the digit &ldquo;2&rdquo; (<code>strpos()</code> in <code>admin/plugins.php</code>). If it is missing, moziloCMS automatically disables the plugin in the admin area.</small></td></tr>  <tr><td><b>PHP</b></td><td>8.1 or higher</td></tr>  <tr><td><b>iconv</b></td><td>Recommended (for accent transliteration in <code>slugify()</code>; always active on standard PHP and IONOS)</td></tr>  <tr><td><b>.htaccess</b></td><td>Sitemap rule and catch-all rules required &ndash; see htaccess_snippet.txt</td></tr></table>
info_install = <b>Important Installation Notes</b><ul>  <li>The plugin is activated in the admin area under <b>Plugins</b> &ndash; no additional call in <code>template.html</code> or in a content page is required.</li>  <li>The <code>.htaccess</code> must be manually extended with the sitemap rule and the catch-all rules from <code>htaccess_snippet.txt</code> &ndash; without these rules, slug URLs will be answered with &ldquo;Not Found&rdquo;.<br /><b>Important:</b> Always enter the rules completely or remove them completely &ndash; a partial configuration can make the admin area inaccessible.</li>  <li>If the required rules are incomplete, incorrect or commented out, the plugin suspends URL rewriting (SEO URLs inactive) and automatically resumes operation as soon as the <code>.htaccess</code> is correctly configured &ndash; no re-activation in the admin area required.</li>  <li>HTTPS and WWW redirects must also be entered in the <code>.htaccess</code> (adjust the domain).</li></ul>
info_features = <b>Features</b><table>  <tr><td><b>Umlaut replacement</b></td><td>ä→ae, ö→oe, ü→ue, ß→ss (+ common accents)</td></tr>  <tr><td><b>Spaces</b></td><td>Replaced by hyphens</td></tr>  <tr><td><b>Link rewriting</b></td><td>All internal <code>href</code> links in the HTML output are rewritten</td></tr>  <tr><td><b>301 redirect</b></td><td>Old paths with umlauts/spaces are permanently redirected</td></tr>  <tr><td><b>Collision protection</b></td><td>Identical slugs automatically receive a suffix (<code>-2</code>, <code>-3</code> …)</td></tr>  <tr><td><b>Sitemap compatible</b></td><td><code>?action=sitemap</code> works unchanged, links are also rewritten</td></tr>  <tr><td><b>MetaKeywordsDescription</b></td><td>Compatible with the MetaKeywordsDescription plugin – individual meta data per page is set correctly</td></tr></table>
info_examples = <b>Examples</b><table>  <tr><td>Original path</td><td>Slug URL</td></tr>  <tr><td><code>/Über Uns/</code></td><td><code>/ueber-uns/</code></td></tr>  <tr><td><code>/Über Uns/Unser Team/</code></td><td><code>/ueber-uns/unser-team/</code></td></tr>  <tr><td><code>/Häufige Fragen/</code></td><td><code>/haeufige-fragen/</code></td></tr></table>
htaccess_ok = &#10003; .htaccess correctly configured.
htaccess_missing = &#9888; .htaccess not found.
htaccess_incomplete = &#9888; SEO URLs inactive: The .htaccess configuration is incomplete.</p><p>The plugin remains active and will automatically resume operation once all required rules are correctly added &ndash; no re-activation in the admin required. Please add rules completely or remove them completely &ndash; a partial configuration can make the admin area inaccessible.
htaccess_required = <b>Required entries in .htaccess:</b>
config_debug = Enable debug mode (slug map available at /?seo_debug=1)
